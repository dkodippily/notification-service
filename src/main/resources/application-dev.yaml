server:
  port: 8282

spring:
  config:
    import: optional:secrets.yaml

configurations:
  service: email
  enrichApi: https://zenquotes.io/api/random
  fromAddress: sendgrid@rakenapp.com
  filterEmailsByDomain: true
  filterDomain: redflag.com

springdoc:
  swagger-ui:
    enabled: true